node_modules
a11y-violations.log
snapshots

# Cypress failure artifacts - never commit
cypress/screenshots
cypress/videos

# Local secrets - never commit. Copy cypress.env.example.json to create it.
cypress.env.json
